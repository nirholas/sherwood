{
  "name": "sherwood",
  "private": true,
  "version": "0.1.0",
  "description": "Halt-aware lending against tokenized equities on Robinhood Chain",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "forge:build": "forge build --root contracts",
    "forge:test": "forge test --root contracts -vv",
    "e2e": "node scripts/e2e.mjs",
    "discover": "node scripts/discover-pools.mjs",
    "abis": "node scripts/emit-abis.mjs",
    "demo": "node scripts/demo.mjs"
  },
  "devDependencies": {
    "viem": "^2.21.0"
  }
}
